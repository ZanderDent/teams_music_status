# Single source of truth for the app's version.
# Read by scripts/build-app.sh and scripts/release.sh — do not duplicate these
# strings anywhere else.
#
# MARKETING_VERSION is what users see (CFBundleShortVersionString).
# BUILD_NUMBER must increase monotonically for every artifact you publish
# (CFBundleVersion). Bump it even for a rebuild of the same marketing version.
MARKETING_VERSION=1.0.2
BUILD_NUMBER=3
